import java.io.*;
import java.util.*;

public class selection {
    //choose a pivot value (random number)
    //partition the list(rearrange so all values less are on left, and greater values on right
    //now pivot is in its final location
    // if k = location, done
    // else, select on Upper or lower half//
}
